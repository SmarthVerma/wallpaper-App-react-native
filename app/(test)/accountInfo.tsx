import { SafeAreaView, Text } from "react-native";

export default function AccountInfo() {
    return <SafeAreaView>
        <Text>
            This is accountInfo
        </Text>
    </SafeAreaView>
}